const catchAsync = require('../utils/catchAsync');
const toJsonApi = require('../utils/toJsonApi');

const getBookingHistory = catchAsync(async () => {

});

module.exports = {
  getBookingHistory,
};
